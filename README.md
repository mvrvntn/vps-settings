# Lightweight VPS Setup for Remnawave

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

Легкий Bash-скрипт для базовой настройки и укрепления безопасности свежеустановленного VPS на базе Debian/Ubuntu, оптимизированный для работы с нодой Remnawave.

## 📋 Описание

Этот скрипт выполняет базовую настройку и укрепление безопасности для свежеустановленного сервера Debian/Ubuntu. Он специально разработан для подготовки VPS к установке ноды Remnawave и не конфликтует со скриптом установки: https://github.com/eGamesAPI/remnawave-reverse-proxy

**Особенности:**
- 🎓 **Интерактивный интерфейс** с понятными описаниями для начинающих
- 🔧 **Неинтерактивный режим** для автоматизации через переменные окружения
- 🛡️ **Без UFW и Fail2Ban** — не конфликтует с Remnawave
- 🌐 **Русскоязычный интерфейс** — все сообщения на русском языке
- ✅ **Совместимость** — протестирован на Debian 11/12 и Ubuntu 20.04/22.04/24.04

## ⚡ Быстрый старт

Запустите скрипт одной командой на вашем сервере:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/mvrvntn/vps-settings/main/setup.sh)
```

## 🚀 Использование

### Интерактивный режим

При запуске без параметров скрипт отобразит интерактивное меню:

```bash
bash setup.sh
```

Меню позволит вам выбрать компоненты для установки:

1. 🎓 **Настроить безопасный SSH** — Изменяет стандартный порт SSH и ужесточает параметры подключения для защиты от ботов.
2. 🎓 **Укрепить и оптимизировать систему** — Применяет базовые параметры безопасности ядра, создает swap и настраивает время.
3. 🎓 **Установить Docker и утилиты** — Устанавливает Docker, Docker Compose и основной набор утилит для администрирования.
4. 🎓 **Установить tblocker** — Блокирует подключения к торрент-трекерам на уровне iptables.
5. 🎓 **Заблокировать ICMP (ping)** — Блокирует входящие ICMP-запросы для скрытия сервера от простого сканирования.
6. 🎓 **Отключить IPv6** — Полностью отключает протокол IPv6 на уровне ядра и загрузчика.

### Неинтерактивный режим

Для автоматического запуска используйте переменные окружения:

```bash
SSH_PORT=1337 INSTALL_TBLOCKER=true BLOCK_ICMP=true DISABLE_IPV6=true TIMEZONE=Europe/Moscow bash setup.sh
```

## 🔧 Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `SSH_PORT` | Порт для SSH-подключения | `2222` |
| `INSTALL_TBLOCKER` | Установить tblocker для блокировки торрентов | `false` |
| `BLOCK_ICMP` | Заблокировать входящие ICMP-запросы (ping) | `false` |
| `DISABLE_IPV6` | Полностью отключить IPv6 | `false` |
| `TIMEZONE` | Установить временную зону (например, `Europe/Moscow`) | пусто (пропуск) |

## ⚠️ Важное предупреждение

**Смена порта SSH:**

После выполнения скрипта порт SSH будет изменен на указанный (по умолчанию 2222). **Важно:**

1. Перед запуском скрипта убедитесь, что вы открыли новый порт в файрволе вашего облачного провайдера (DigitalOcean, AWS, Hetzner и т.д.)
2. Если вы не откроете новый порт, вы **потеряете доступ** к серверу!
3. После изменения порта подключайтесь к серверу с использованием нового порта:
   ```bash
   ssh root@ваш-ip -p 2222
   ```

## 📦 Что устанавливается

### Обязательные компоненты

- **Настройка SSH:**
  - Изменение порта SSH
  - Разрешение входа для root по паролю
  - Отключение интерактивной аутентификации
  - Установка лимитов на попытки входа

- **Укрепление системы:**
  - Параметры безопасности ядра (защита от IP-спуфинга, SYN-cookie)
  - Увеличение лимита отслеживаемых соединений
  - Создание swap-файла размером 2GB
  - Установка и настройка chrony для синхронизации времени
  - Автоматические обновления безопасности

- **Установка ПО:**
  - Docker и Docker Compose последней версии
  - Базовые утилиты: `htop`, `mc`, `curl`, `wget`, `git`, `ncdu`, `iptables-persistent`

### Опциональные компоненты

- **tblocker** — блокировка торрентов через iptables
- **Блокировка ICMP** — скрытие сервера от ping-запросов
- **Отключение IPv6** — полное отключение протокола IPv6

## 🔒 Совместимость с Remnawave

Скрипт протестирован и полностью совместим с нодой Remnawave. Он не устанавливает UFW или Fail2Ban, что позволяет Remnawave корректно работать без конфликтов с сетевыми правилами.

## 📋 Требования

- Операционная система:
  - Debian 11 (Bullseye) или 12 (Bookworm)
  - Ubuntu 20.04 LTS, 22.04 LTS или 24.04 LTS
- Права root или sudo
- Интернет-соединение

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. Подробности см. в файле [LICENSE](LICENSE).

## 🤝 Вклад

Вклад приветствуется! Если вы нашли ошибку или хотите предложить улучшение, создайте issue или pull request.

## ⚠️ Отказ от ответственности

Этот скрипт предоставляется "как есть" без каких-либо гарантий. Авторы не несут ответственности за любой ущерб, причиненный использованием этого скрипта. Всегда делайте резервные копии важных данных перед запуском.

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории GitHub.

---

**Автор:** Kilo Code  
**Версия:** 1.0.0
